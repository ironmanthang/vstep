brainstorm with me:
# on mobile:
- why have the "xin chào, ..." banner?? should we delete it ? also what is the logic of the "chuỗi"??or is it just hardcode text?
- should we delete the text "Hoạt Động Hôm Nay" ?
- i want to re-order: Trang chủ, Từ vựng SRS, Luyện kỹ năng, Thi thử, and delete: Hồ sơ & Cài đặt
- in the page home, i just want the best simplistic ui/ux, big area for: the srs, skills, mock test, process, all without scrolling 
- i want to delete Hồ sơ & Cài đặt because i want clicking on the gmail icon avt on top right will jump to that page, but i want that page only have full gmail, change name, thats it, all the other stuffs, delete it
- same as above, in page Luyện kỹ năng i dont want user to scrol, as i only want that page to present the 4 skill, no unnecessary texts, no "cần củng cố từ vựng...", maybe just symbol and the text ò the skill in english would be enough
- in page srs, move the rết deck to when click on the gmail avt top right
- in dark mode, i want the top banner (where show phone system info) to be dark kinda like the website bg color
# on desktop:

# on both:
i want the option that user chose [FlashcardPage.tsx#L185-185](textBlock;file:///d%3A/program/vstep/src/features/flashcard/FlashcardPage.tsx#L185-185)  Bậc CEFR to be persistent session (local device, meaning next time log in on the same device the option about choosing b1/c1 stay persist)
